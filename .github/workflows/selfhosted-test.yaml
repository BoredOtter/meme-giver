name: Selfhosted Actions Runner
on:
  workflow_dispatch:

jobs:
  Explore-GitHub-Actions:
    runs-on: arc-runners-meme-giver
    steps:
    - run: echo "🎉 This job uses runner scale set runners!"
